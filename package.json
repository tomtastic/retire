{
  "name": "retirement-drawdown-modeller",
  "private": true,
  "version": "1.0.0",
  "description": "Dependency-free UK retirement drawdown modeller",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:coverage": "node --test --experimental-test-coverage --test-coverage-include=model.js tests/*.test.js"
  }
}
